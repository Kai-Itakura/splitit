model User {
  id       Int     @id @default(autoincrement())
  email    String  @unique
  password String
  name     String?

  UserGroups    Group[]
  Expenses      Expense[]
  ExpenseShares ExpenseShare[]
  refreshTokens RefreshToken[]
}
