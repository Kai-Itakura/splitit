model Expense {
  id     Int    @id @default(autoincrement())
  title  String
  amount Int

  payerId String
  payer   User   @relation(fields: [payerId], references: [id])

  groupId Int
  group   EventGroup @relation(fields: [groupId], references: [id])

  expensesShares ExpenseShare[]
}
