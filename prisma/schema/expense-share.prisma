model ExpenseShare {
  id     Int @id @default(autoincrement())
  amount Int

  expneseId Int
  expense   Expense @relation(fields: [expneseId], references: [id])

  receiverId Int
  user       User @relation(fields: [receiverId], references: [id])
}
